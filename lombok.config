# Marca el código generado por Lombok con @lombok.Generated para que JaCoCo lo excluya
# de la cobertura (evita penalizar getters/equals/builders en la métrica por clase).
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
